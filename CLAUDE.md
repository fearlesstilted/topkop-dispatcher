# Контекст Проекта
TOPKOP RAG Dispatcher — веб-виджет для сайта строительной компании TOP KOP (Gołdap).
Цель: Сбор первичных лидов (услуга, объем, локация, телефон) и передача их руководству.

# Моя роль и твой стиль общения
Я соло-разработчик, который делает MVP для CEO в сжатые сроки. 
Твоя роль — мой Senior Tech Lead. 
- Пиши код чисто, надежно и без оверинжиниринга.
- ВСЕГДА пиши короткие комментарии к сложным строкам кода, чтобы я учился.
- Если я прошу сделать что-то опасное для дедлайна — жестко останавливай меня.

# Стек технологий
- Python 3.10+, Gradio 6 (UI виджета, потоковый вывод SSE)
- AsyncOpenAI API (строго LLaMA-3 через Groq): основная llama-3.3-70b-versatile, маркетинг llama-3.1-8b-instant
- httpx (async HTTP для Make.com webhook, НЕ requests)
- Make.com webhook → Google Sheets + Telegram менеджеру
- База знаний: локальный `KnowledgeTopKop.json`
- Shadow DOM виджет (widget.js) — изоляция от CSS сайта клиента

# Жесткие архитектурные правила (НЕ НАРУШАТЬ)
1. **Только Async:** Сервер на Gradio должен держать несколько клиентов. Запрещено использовать синхронные вызовы `client.chat.completions`. Переписать всё на `AsyncOpenAI`, `async def`, `await`.
2. **Убить CSV (Интеграция лидов):** Категорически запрещено использовать `csv.writer` и сохранять лиды локально. Все лиды из `_marketing_worker` должны улетать JSON-пакетом через POST-запрос на вебхук Make.com.
3. **Единый источник истины (`config.py`):** Сейчас `app_web.py` читает переменные напрямую. При рефакторинге все константы должны импортироваться СТРОГО из `config.py`.
4. **LLM JSON Mode:** Для функции маркетингового анализа всегда использовать `response_format={"type": "json_object"}`. Запрещено использовать костыли на регулярках (`extract_text`) для очистки ответа маркетинговой модели.

# Воркфлоу в Claude Code
- Зависимость `gradio` должна быть добавлена в `requirements.txt`.
- Баг-фиксы и мелкие правки — делать сразу без подтверждения. Крупные рефакторинги — показать diff перед правкой.
- CLAUDE.md обновлять самостоятельно каждые ~12 промптов: выжимка новых фактов о бизнесе/стеке/решениях.
- Задавать вопросы по бизнесу и продукту проактивно — пользователь поощряет совместное мышление.
- НЕ добавлять Co-Authored-By в git commit — пользователь явно запретил.

# Триггер "клубничка"
Когда пользователь пишет слово "клубничка" — немедленно сохранить краткую выжимку текущего разговора в файл `C:\Users\Kierownik Produkcji\.claude\projects\C--TopKopRCMcore\memory\session_ДАТА.md` (ДАТА = текущая дата в формате YYYY-MM-DD). Формат выжимки: что обсуждали, что сделали, что решили, что осталось открытым. Потом сообщить пользователю что файл сохранён.

# Контекст бизнеса (обновлено 2026-04-09)
- Группа из ~5 компаний: TOP KOP (стройка/земляные работы), RCM Defence (металл, обработка стали, гос. контракты), rozliczeniowa и др.
- TOP KOP берётся за нестандартные заказы: прицепы, техника до 60k EUR, посредничество
- Основной канал связи в фирме — WhatsApp (не Telegram, не email)
- Make.com pipeline: Webhook → Google Sheets (дедупликация через Router) → WhatsApp менеджеру
- Пользователь — единственный технарь в компании, роль: automatyzacja procesów, прямой контакт с CEO
- CEO не технический, показывать MVP нужно просто: "вот чат, попробуй"
- Маркетинг-девочка правит WordPress и Canva — ей нужно будет вставить widget.js тег на сайт